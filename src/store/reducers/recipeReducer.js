const initialState = {
    recipes:[],
    reviews:[]
}

export function recipeReducer(state = initialState, action = {}){
    switch (action.type){
        default:
            return state; 
    }
}